const express = require('express'); const auth = require('../../middleware/platformAuthMiddleware'); const controller = require('./paymentSubmissions.controller'); const router = express.Router(); router.use(auth); router.get('/', controller.list); router.post('/:id/approve', controller.approve); router.post('/:id/reject', controller.reject); module.exports = router;
